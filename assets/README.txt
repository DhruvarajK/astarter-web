Copy these files from your original Vite `dist/` (or `public/`) into this `assets/` folder so paths match `index.html`:

  logo-DKIUXeE4.png
  core-BRYOy9yX.png
  bgv-B-W2cvY7.webm   (hero background — without it the video area stays dark)
  bgv-B8k_vcy5.mp4
  partner-*.png       (all partner logos you use; `js/app.js` lists filenames — add new names there if needed)

`newsletter-bg.png` is already generated in this rebuild folder.

Optional: place `MiniPC.glb` in `../models/` for the Three.js viewers (see `models/README.txt`).
